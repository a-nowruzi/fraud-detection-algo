# سیستم تشخیص تقلب پزشکی - پورتال کاربری

این پروژه یک پورتال کاربری مدرن برای سیستم تشخیص تقلب پزشکی است که از **Minia Template** استفاده می‌کند.

## 🎨 ویژگی‌های Minia Template

### طراحی مدرن و زیبا
- **Header ثابت** با لوگو، جستجو و منوی کاربری
- **Sidebar عمودی** با منوی ناوبری و کارت ارتقا
- **Right Sidebar** برای تنظیمات تم و شخصی‌سازی
- **Footer** با اطلاعات کپی‌رایت

### تم‌های رنگی
- **Light Mode**: تم روشن پیش‌فرض
- **Dark Mode**: تم تیره برای استفاده در شب
- **Theme Switcher**: تغییر آسان بین تم‌ها

### ویژگی‌های تعاملی
- **Responsive Design**: سازگار با تمام دستگاه‌ها
- **Mobile Sidebar**: منوی همبرگری برای موبایل
- **Search Bar**: جستجوی پیشرفته در سیستم
- **Notifications**: سیستم اعلان‌ها
- **User Profile**: پروفایل کاربری

## 🚀 نصب و راه‌اندازی

### پیش‌نیازها
- Node.js (نسخه 18 یا بالاتر)
- Yarn یا npm

### مراحل نصب
```bash
# نصب dependencies
yarn install

# اجرای پروژه در حالت توسعه
yarn dev

# ساخت نسخه تولید
yarn build

# پیش‌نمایش نسخه تولید
yarn preview
```

## 📁 ساختار پروژه

```
portal/
├── src/
│   ├── assets/
│   │   ├── css/
│   │   │   └── app.min.css          # استایل‌های Minia
│   │   └── js/
│   │       └── app.js               # JavaScript های Minia
│   ├── components/
│   │   └── Layout.tsx               # Layout اصلی با Minia
│   ├── pages/                       # صفحات مختلف
│   ├── services/                    # سرویس‌های API
│   ├── App.tsx                      # کامپوننت اصلی
│   └── main.tsx                     # نقطه ورودی
├── public/                          # فایل‌های استاتیک
├── package.json                     # dependencies
└── README.md                        # مستندات
```

## 🎯 صفحات موجود

1. **Dashboard** (`/`) - صفحه اصلی با آمار کلی
2. **تشخیص تقلب** (`/predict`) - فرم تشخیص تقلب
3. **نمودارها** (`/charts`) - نمایش نمودارهای آماری
4. **آمار سیستم** (`/stats`) - آمار تفصیلی سیستم

## 🎨 شخصی‌سازی

### تغییر رنگ‌ها
رنگ‌های اصلی در فایل `src/assets/css/app.min.css` در بخش `:root` تعریف شده‌اند:

```css
:root {
  --primary-color: #556ee6;      /* رنگ اصلی */
  --secondary-color: #6c757d;    /* رنگ ثانویه */
  --success-color: #34c38f;      /* رنگ موفقیت */
  --danger-color: #f46a6a;       /* رنگ خطا */
  /* ... */
}
```

### تغییر فونت
فونت پیش‌فرض Inter است که در `index.html` لینک شده است. می‌توانید آن را تغییر دهید.

### اضافه کردن آیکون‌ها
پروژه از `lucide-react` برای آیکون‌ها استفاده می‌کند. برای اضافه کردن آیکون جدید:

```tsx
import { NewIcon } from 'lucide-react';

// استفاده در کامپوننت
<NewIcon className="h-4 w-4" />
```

## 📱 Responsive Design

Template به طور کامل responsive است و در تمام اندازه‌های صفحه به خوبی کار می‌کند:

- **Desktop**: نمایش کامل sidebar و header
- **Tablet**: sidebar قابل جمع شدن
- **Mobile**: sidebar همبرگری و overlay

## 🌙 Dark Mode

سیستم dark mode به صورت خودکار تنظیمات کاربر را ذخیره می‌کند:

- تغییر تم از دکمه ماه/خورشید در header
- ذخیره تنظیمات در localStorage
- بارگذاری خودکار تم در بازدید بعدی

## 🔧 تنظیمات پیشرفته

### Right Sidebar
- تغییر layout (عمودی/افقی)
- تنظیم اندازه sidebar
- تغییر رنگ‌ها و تم‌ها

### Sidebar
- منوی ناوبری اصلی
- کارت ارتقا
- دسته‌بندی منوها

## 📊 کامپوننت‌های موجود

- **Layout**: Layout اصلی با Minia template
- **Dashboard**: صفحه داشبورد
- **PredictionForm**: فرم تشخیص تقلب
- **ChartsGallery**: نمایش نمودارها
- **StatsDashboard**: آمار سیستم
- **RiskIndicators**: شاخص‌های ریسک

## 🚀 توسعه

### اضافه کردن صفحه جدید
1. فایل جدید در پوشه `pages` ایجاد کنید
2. Route جدید در `App.tsx` اضافه کنید
3. لینک جدید در `Layout.tsx` اضافه کنید

### تغییر استایل‌ها
- استایل‌های عمومی در `app.min.css`
- استایل‌های خاص کامپوننت در فایل SCSS مربوطه

## 📝 نکات مهم

- تمام متن‌ها به فارسی نوشته شده‌اند
- جهت صفحه RTL است
- از CSS Variables برای رنگ‌ها استفاده می‌شود
- JavaScript در `app.js` برای عملکردهای پایه

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Pull Request ارسال کنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

---

**توسعه یافته با ❤️ توسط تیم توسعه**
