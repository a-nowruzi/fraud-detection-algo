# سیستم تشخیص تقلب پزشکی - قالب Minia

این پروژه از قالب Minia برای رابط کاربری استفاده می‌کند که یک قالب مدیریتی مدرن و زیبا است.

## ویژگی‌های قالب Minia

### 🎨 طراحی مدرن
- رابط کاربری زیبا و حرفه‌ای
- پشتیبانی از حالت تاریک و روشن
- طراحی واکنش‌گرا برای تمام دستگاه‌ها
- انیمیشن‌های نرم و جذاب

### 🧭 ناوبری هوشمند
- منوی کناری عمودی قابل تنظیم
- نوار بالای صفحه با جستجو و اعلان‌ها
- نوار کناری راست برای تنظیمات قالب
- منوی همبرگری برای موبایل

### 🎯 کامپوننت‌های آماده
- کارت‌های آماری زیبا
- نمودارها و چارت‌ها
- جداول داده
- فرم‌های کاربرپسند
- دکمه‌ها و آیکون‌ها

### 🌙 حالت تاریک
- تغییر خودکار بین حالت روشن و تاریک
- ذخیره تنظیمات در localStorage
- رنگ‌بندی بهینه برای هر حالت

## ساختار فایل‌ها

```
portal/
├── src/
│   ├── assets/
│   │   ├── css/
│   │   │   ├── app.min.css          # استایل‌های اصلی Minia
│   │   │   ├── bootstrap.min.css    # Bootstrap CSS
│   │   │   ├── icons.min.css        # آیکون‌ها
│   │   │   ├── preloader.min.css    # صفحه بارگذاری
│   │   │   ├── custom.css           # استایل‌های سفارشی
│   │   │   ├── rtl.css              # پشتیبانی RTL
│   │   │   ├── fonts.css            # فونت‌ها
│   │   │   └── font-variables.css   # متغیرهای فونت
│   │   └── js/
│   │       └── app.js               # JavaScript قالب
│   ├── components/
│   │   └── Layout.tsx               # لایوت اصلی با قالب Minia
│   └── pages/
│       └── Dashboard.tsx            # صفحه داشبورد با قالب Minia
├── index.html                       # HTML اصلی با قالب Minia
└── package.json
```

## نحوه استفاده

### 1. اجرای پروژه
```bash
cd portal
npm install
npm run dev
```

### 2. تغییر حالت قالب
- کلیک روی دکمه ماه/خورشید در نوار بالا برای تغییر حالت تاریک/روشن
- کلیک روی دکمه تنظیمات برای باز کردن نوار کناری راست
- انتخاب طرح‌بندی عمودی یا افقی

### 3. سفارشی‌سازی
- فایل `custom.css` برای تغییرات استایل
- فایل `Layout.tsx` برای تغییر ساختار لایوت
- فایل `app.js` برای تغییرات JavaScript

## کامپوننت‌های کلیدی

### Header
- لوگو و نام سیستم
- جستجو
- تغییر حالت تاریک/روشن
- اعلان‌ها
- پروفایل کاربر

### Sidebar
- منوی اصلی
- زیرمنوها
- کارت ارتقا
- پشتیبانی از آیکون‌ها

### Main Content
- عنوان صفحه
- breadcrumb
- محتوای اصلی
- کارت‌های آماری

### Footer
- اطلاعات کپی‌رایت
- لینک‌های مفید

## ویژگی‌های RTL

- پشتیبانی کامل از زبان فارسی
- جهت‌بندی راست به چپ
- فونت‌های فارسی
- تنظیمات مناسب برای RTL

## پشتیبانی موبایل

- منوی همبرگری
- طراحی واکنش‌گرا
- بهینه‌سازی برای صفحات کوچک
- لمس‌پسند

## مرورگرهای پشتیبانی شده

- Chrome (جدیدترین نسخه)
- Firefox (جدیدترین نسخه)
- Safari (جدیدترین نسخه)
- Edge (جدیدترین نسخه)

## نکات مهم

1. **فونت‌ها**: فونت‌های Estedad برای فارسی استفاده می‌شود
2. **آیکون‌ها**: از Lucide React برای آیکون‌ها استفاده می‌شود
3. **رنگ‌ها**: متغیرهای CSS برای تغییر آسان رنگ‌ها
4. **انیمیشن‌ها**: انیمیشن‌های نرم برای تجربه کاربری بهتر

## عیب‌یابی

### مشکل نمایش قالب
- مطمئن شوید تمام فایل‌های CSS بارگذاری شده‌اند
- کنسول مرورگر را برای خطاها بررسی کنید
- فایل‌های JavaScript را بررسی کنید

### مشکل RTL
- مطمئن شوید `dir="rtl"` در HTML تنظیم شده
- فایل `rtl.css` بارگذاری شده باشد
- فونت‌های فارسی نصب شده باشند

### مشکل موبایل
- منوی همبرگری کار نمی‌کند
- نوار کناری باز نمی‌شود
- طراحی واکنش‌گرا نیست

## پشتیبانی

برای سوالات و مشکلات مربوط به قالب Minia، لطفاً با تیم توسعه تماس بگیرید.

---

**توسعه‌دهنده**: دکتر مجتبی فرخ  
**تاریخ**: 2025  
**نسخه**: 1.0.0
