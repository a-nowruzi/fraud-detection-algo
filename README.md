# سیستم تشخیص تقلب پزشکی

این پروژه یک سیستم کامل تشخیص تقلب در نسخه‌های پزشکی است که شامل backend (Python/Flask) و frontend (React/TypeScript) می‌باشد.

## 🏗️ ساختار پروژه

```
fraud-detection-algo/
├── api/                    # Backend (Python/Flask)
│   ├── app.py             # سرور اصلی Flask
│   ├── requirements.txt   # وابستگی‌های Python
│   ├── *.py              # توابع محاسباتی
│   └── README.md         # مستندات Backend
├── portal/                # Frontend (React/TypeScript)
│   ├── src/              # کدهای React
│   ├── package.json      # وابستگی‌های Node.js
│   └── README.md         # مستندات Frontend
└── README.md             # این فایل
```

## 🚀 راه‌اندازی سریع

### پیش‌نیازها

- **Python 3.8+** برای Backend
- **Node.js 16+** برای Frontend
- **npm** یا **yarn**

### 1. راه‌اندازی Backend

```bash
# رفتن به پوشه backend
cd api

# نصب وابستگی‌ها
pip install -r requirements.txt

# اجرای سرور
python app.py
```

Backend روی `http://localhost:5000` اجرا می‌شود.

### 2. راه‌اندازی Frontend

```bash
# رفتن به پوشه frontend
cd portal

# نصب وابستگی‌ها
npm install

# اجرای برنامه
npm run dev
```

Frontend روی `http://localhost:5173` اجرا می‌شود.

## 📋 ویژگی‌های سیستم

### 🔍 تشخیص تقلب
- الگوریتم Isolation Forest برای تشخیص تقلب
- محاسبه ۱۱ شاخص ریسک مختلف
- پشتیبانی از تاریخ شمسی
- محاسبه سن بر اساس تاریخ تولد

### 📊 نمودارهای تحلیلی
- نمودار تقلب بر اساس استان
- نمودار تقلب بر اساس جنسیت و گروه سنی
- نمودارهای زمانی و روند
- نمودارهای مربوط به بیمه و پرونده

### 📈 داشبورد آمار
- آمار کلی سیستم
- اطلاعات مدل و عملکرد
- نسبت‌ها و درصدها

### 🎨 رابط کاربری
- طراحی زیبا و مدرن
- پشتیبانی کامل از زبان فارسی
- واکنش‌گرا برای تمام دستگاه‌ها
- تجربه کاربری بهینه

## 🔧 API Endpoints

### تشخیص تقلب
- `POST /predict` - تشخیص تقلب برای نسخه جدید

### آمار و اطلاعات
- `GET /stats` - آمار کلی سیستم
- `GET /health` - بررسی سلامت سیستم

### نمودارها
- `GET /charts/fraud-by-province` - نمودار تقلب بر اساس استان
- `GET /charts/fraud-by-gender` - نمودار تقلب بر اساس جنسیت
- `GET /charts/fraud-by-age` - نمودار تقلب بر اساس گروه سنی
- `GET /charts/risk-indicators` - نمودار شاخص‌های ریسک
- و سایر نمودارهای تحلیلی...

## 🛠️ تکنولوژی‌های استفاده شده

### Backend
- **Python 3.8+**
- **Flask** - فریم‌ورک وب
- **Scikit-learn** - یادگیری ماشین
- **Pandas** - پردازش داده
- **NumPy** - محاسبات عددی
- **Matplotlib** - تولید نمودار
- **Flasgger** - مستندات API

### Frontend
- **React 19** - کتابخانه UI
- **TypeScript** - زبان برنامه‌نویسی
- **Vite** - ابزار build
- **Tailwind CSS** - فریم‌ورک CSS
- **React Router** - مدیریت routing
- **Axios** - HTTP client
- **Lucide React** - آیکون‌ها

## 📖 نحوه استفاده

### 1. تشخیص تقلب
1. به صفحه "تشخیص تقلب" بروید
2. اطلاعات نسخه را وارد کنید:
   - شماره بیمار
   - تاریخ تولد (شمسی)
   - تاریخ پذیرش (شمسی)
   - نوع خدمت
   - نام پزشک
   - تخصص پزشک
   - مبلغ
3. روی "تشخیص تقلب" کلیک کنید
4. نتیجه و شاخص‌های ریسک را مشاهده کنید

### 2. مشاهده نمودارها
1. به صفحه "نمودارها" بروید
2. نمودار مورد نظر را انتخاب کنید
3. پیش‌نمایش نمودار را مشاهده کنید

### 3. بررسی آمار
1. به صفحه "آمار سیستم" بروید
2. آمار کلی و عملکرد سیستم را مشاهده کنید

## 🔍 شاخص‌های ریسک

سیستم از ۱۱ شاخص مختلف برای تشخیص تقلب استفاده می‌کند:

1. **نسبت کل پزشکان به پزشکان منحصر به فرد**
2. **نسبت کل بیماران به بیماران منحصر به فرد**
3. **درصد تغییر هزینه پزشک**
4. **درصد تغییر هزینه بیمار**
5. **درصد تفاوت هزینه خدمت**
6. **درصد تفاوت هزینه خدمت برای پزشک**
7. **درصد تفاوت هزینه تخصص برای پزشک**
8. **درصد تفاوت هزینه تخصص مستقیم**
9. **درصد تفاوت هزینه خدمت برای بیمار**
10. **درصد تفاوت هزینه خدمت کلی**
11. **نسبت خدمت**

## 🚨 عیب‌یابی

### خطای اتصال به API
- مطمئن شوید backend روی پورت 5000 اجرا می‌شود
- بررسی کنید که CORS در backend فعال باشد
- از کامپوننت "تست اتصال به API" در داشبورد استفاده کنید

### خطای نصب وابستگی‌ها
- Node.js و Python را به‌روزرسانی کنید
- پوشه‌های `node_modules` و `__pycache__` را حذف کرده و دوباره نصب کنید

### خطای مدل
- مطمئن شوید فایل‌های داده در پوشه `api/` موجود هستند
- بررسی کنید که تمام وابستگی‌های Python نصب شده‌اند

## 📝 توسعه

### Backend
```bash
cd api
# نصب وابستگی‌های توسعه
pip install -r requirements.txt

# اجرا در حالت توسعه
python app.py
```

### Frontend
```bash
cd portal
# نصب وابستگی‌ها
npm install

# اجرا در حالت توسعه
npm run dev

# ساخت نسخه تولید
npm run build

# بررسی کد
npm run lint
```

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 🤝 مشارکت

برای مشارکت در توسعه این پروژه:

1. پروژه را fork کنید
2. یک branch جدید ایجاد کنید
3. تغییرات خود را commit کنید
4. Pull Request ارسال کنید

## 📞 پشتیبانی

برای گزارش مشکلات یا درخواست ویژگی‌های جدید، لطفاً یک Issue ایجاد کنید.
